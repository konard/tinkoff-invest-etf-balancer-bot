/**
 * Заглушка для получения капитализации акций
 * TODO: реализовать получение капитализации акций
 */
export const getShareMarketCapRUB = async (ticker: string): Promise<{ marketCapRUB?: number } | null> => {
  // Пока возвращаем null, чтобы не ломать логику
  // В будущем можно реализовать получение капитализации акций
  return null;
};
